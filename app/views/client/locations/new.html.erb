<% if @errors %>
  <ul>
    <% @errors.each do |error| %>
      <li><%= error %></li>
    <% end %>
  </ul>
<% end %>

<%= form_tag "/client/locations", method: :post do %>
  <div>
    Name: <input type="text" name="name" value="<%= @location['name'] %>" />
  </div>
  <div>
    Longitude: <input type="text" name="longitude" value="<%= @location['longitude'] %>" />
  </div>
  <div>
    Latitude: <input type="text" name="latitude" value="<%= @location['latitude'] %>" />
  </div>
  
  <input type="submit" name="" value="Create Location" />
<% end %>